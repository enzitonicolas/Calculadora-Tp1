#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
#endif // FUNCIONES_H_INCLUDED
/**
*\brief Solicita dos operandos al usuario y luego devuelve el resultado.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la suma entre num1 y num2.
*/
int sumar(int num1,int num2);
/**
*\brief Solicita dos operandos, verifica y devuelve true resultado o false advertencia.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la suma entre num1 y num2.
*/
void calcusumar(int num1,int num2);
/**
*\brief Solicita dos operandos al usuario y luego devuelve el resultado.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la resta entre num1 y num2.
*/
int restar(int num1,int num2);
/**
*\brief Solicita dos operandos, verifica y devuelve true resultado o false advertencia.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la resta entre num1 y num2.
*/
void calcurestar(int num1,int num2);
/**
*\brief Solicita dos operandos al usuario y luego devuelve el resultado.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la multiplicacion entre num1 y num2.
*/
int multip(int num1,int num2);
/**
*\brief Solicita dos operandos, verifica y devuelve true resultado o false advertencia.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la multiplicacion entre num1 y num2.
*/
void calcumultip(int num1,int num2);
/**
*\brief Solicita dos operandos al usuario y luego devuelve el resultado.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la division entre num1 y num2.
*/
float divid(float num1,float num2);
/**
*\brief Solicita dos operandos, verifica y devuelve true resultado o false advertencia.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la division entre num1 y num2.
*/
void calcudivid(int num1,int num2);
/**
*\brief Solicita al operando A que ingreso el usuario y luego devuelve el resultado.
*\param num1 solicita el operando A.
*\return El resultado del factorial de num1.
*/
float factorial(float num1);
/**
*\brief Solicita dos operandos y los verifica si son true o false.
*\param num1 solicita el operando A.
*\param num2 solicita el operando B.
*\return El resultado de la suma entre num1 y num2.
*/
int verificacion(int,int);
